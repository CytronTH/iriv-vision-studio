{
  "name": "iriv-model-studio",
  "version": "1.0.0",
  "description": "All-in-one GUI Tool for training and compiling Hailo .hef models",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"wait-on http://localhost:5174 && electron .\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python main.py",
    "build": "cd frontend && npm run build && electron-builder",
    "start": "electron ."
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "electron": "^31.0.0",
    "electron-builder": "^24.0.0",
    "wait-on": "^7.2.0"
  },
  "build": {
    "appId": "com.iriv.model-studio",
    "productName": "IRIV Model Studio",
    "win": {
      "target": "nsis",
      "icon": "frontend/public/icon.ico"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
